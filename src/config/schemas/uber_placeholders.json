{
    "inline": [
        { "name": "$cp", "type": "vec3", "desc": "デフォルトドメイン" },
        { "name": "$cppos", "type": "vec3", "desc": "デフォルトドメインの位置値 (read only)" },
        { "name": "$cprot", "type": "vec3", "desc": "デフォルトドメインの回転値 (read only)" },
        { "name": "$cpt", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$cptr", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" },
        { "name": "$pp", "type": "vec3", "desc": "親ドメイン" },
        { "name": "$pppos", "type": "vec3", "desc": "親ドメインの位置値 (read only)" },
        { "name": "$pprot", "type": "vec3", "desc": "親ドメインの回転値 (read only)" },
        { "name": "$ppt", "type": "vec3", "desc": "変換された親ドメイン" },
        { "name": "$pptr", "type": "vec3", "desc": "変換/回転された親ドメイン" }
    ],
    "argument": [
        { "name": "$cp", "type": "vec3", "desc": "デフォルトドメイン" },
        { "name": "$cppos", "type": "vec3", "desc": "デフォルトドメインの位置値 (read only)" },
        { "name": "$cprot", "type": "vec3", "desc": "デフォルトドメインの回転値 (read only)" },
        { "name": "$cpt", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$cptr", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" },
        { "name": "$pp", "type": "vec3", "desc": "親ドメイン" },
        { "name": "$pppos", "type": "vec3", "desc": "親ドメインの位置値 (read only)" },
        { "name": "$pprot", "type": "vec3", "desc": "親ドメインの回転値 (read only)" },
        { "name": "$ppt", "type": "vec3", "desc": "変換された親ドメイン" },
        { "name": "$pptr", "type": "vec3", "desc": "変換/回転された親ドメイン" },
        { "name": "$var", "type": "float", "desc": "結果を格納する変数" }
    ],
    "sdf2d": [
        { "name": "$cp", "type": "vec3", "desc": "デフォルトドメイン" },
        { "name": "$cppos", "type": "vec3", "desc": "デフォルトドメインの位置値 (read only)" },
        { "name": "$cprot", "type": "vec3", "desc": "デフォルトドメインの回転値 (read only)" },
        { "name": "$cpt", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$cptr", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" },
        { "name": "$pp", "type": "vec3", "desc": "親ドメイン" },
        { "name": "$pppos", "type": "vec3", "desc": "親ドメインの位置値 (read only)" },
        { "name": "$pprot", "type": "vec3", "desc": "親ドメインの回転値 (read only)" },
        { "name": "$ppt", "type": "vec3", "desc": "変換された親ドメイン" },
        { "name": "$pptr", "type": "vec3", "desc": "変換/回転された親ドメイン" },
        { "name": "$d", "type": "float", "desc": "距離を格納する変数" },
        { "name": "$res", "type": "vec4", "desc": "結果を格納する変数" },
        { "name": "$dim", "type": "vec3", "desc": "UIバウンドの寸法 (read only)" },
        { "name": "$prp", "type": "vec3", "desc": "カスタムプロパティ (read only)" },
        { "name": "$tp", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$trp", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" }
    ],
    "sdf3d": [
        { "name": "$cp", "type": "vec3", "desc": "デフォルトドメイン" },
        { "name": "$cppos", "type": "vec3", "desc": "デフォルトドメインの位置値 (read only)" },
        { "name": "$cprot", "type": "vec3", "desc": "デフォルトドメインの回転値 (read only)" },
        { "name": "$cpt", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$cptr", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" },
        { "name": "$pp", "type": "vec3", "desc": "親ドメイン" },
        { "name": "$pppos", "type": "vec3", "desc": "親ドメインの位置値 (read only)" },
        { "name": "$pprot", "type": "vec3", "desc": "親ドメインの回転値 (read only)" },
        { "name": "$ppt", "type": "vec3", "desc": "変換された親ドメイン" },
        { "name": "$pptr", "type": "vec3", "desc": "変換/回転された親ドメイン" },
        { "name": "$d", "type": "float", "desc": "距離を格納する変数" },
        { "name": "$res", "type": "vec4", "desc": "結果を格納する変数" },
        { "name": "$dim", "type": "vec3", "desc": "UIバウンドの寸法 (read only)" },
        { "name": "$prp", "type": "vec3", "desc": "カスタムプロパティ (read only)" },
        { "name": "$tp", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$trp", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" }
    ],
    "ik": [
        { "name": "$trgt", "type": "vec3", "desc": "ターゲットボーン" },
        { "name": "$pol", "type": "vec3", "desc": "ポールボーン" },
        { "name": "$ika", "type": "IKArmature", "desc": "IKアーマチュア構造 (read only)" },
        { "name": "$ika_base", "type": "vec3", "desc": "ルートのドメイン (read only)" },
        { "name": "$root_pos", "type": "vec3", "desc": "ルートボーンの位置値 (read only)" },
        { "name": "$pole_pos", "type": "vec3", "desc": "ポールボーンの位置値 (read only)" },
        { "name": "$target_pos", "type": "vec3", "desc": "ターゲットボーンの位置値 (read only)" },
        { "name": "$length_a", "type": "float", "desc": "ルートボーンの長さ (read only)" },
        { "name": "$length_b", "type": "float", "desc": "IK ポーンの長さ (read only)" },
        { "name": "$key_target", "type": "float", "desc": "TRV経由のターゲット位置 (read only)" },
        { "name": "$key_pole", "type": "float", "desc": "TRV経由のポール位置 (read only)" }
    ],
    "pmod": [
        { "name": "$cp", "type": "vec3", "desc": "デフォルトドメイン" },
        { "name": "$cppos", "type": "vec3", "desc": "デフォルトドメインの位置値 (read only)" },
        { "name": "$cprot", "type": "vec3", "desc": "デフォルトドメインの回転値 (read only)" },
        { "name": "$cpt", "type": "vec3", "desc": "変換されたデフォルトドメイン" },
        { "name": "$cptr", "type": "vec3", "desc": "変換/回転されたデフォルトドメイン" },
        { "name": "$pp", "type": "vec3", "desc": "親ドメイン" },
        { "name": "$pppos", "type": "vec3", "desc": "親ドメインの位置値 (read only)" },
        { "name": "$pprot", "type": "vec3", "desc": "親ドメインの回転値 (read only)" },
        { "name": "$ppt", "type": "vec3", "desc": "変換された親ドメイン" },
        { "name": "$pptr", "type": "vec3", "desc": "変換/回転された親ドメイン" }
    ]
}

